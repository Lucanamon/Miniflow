export const environment = {
  production: false,
  // For local development: use proxy OR direct backend URL
  // Option 1: Use proxy (if backend is localhost) - recommended for dev
  apiUrl: '/api',
  // Option 2: Use direct backend URL (uncomment if not using proxy)
  // apiUrl: 'http://localhost:3000/api',
};
