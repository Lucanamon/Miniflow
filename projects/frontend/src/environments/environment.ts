export const environment = {
  production: false,
  // For local development: use proxy OR direct backend URL
  // Option 1: Use proxy (if backend is localhost)
  // apiUrl: '/api',
  // Option 2: Use direct online backend URL
  apiUrl: 'https://api.mybackend.com/api', // Replace with your actual backend URL
  // Examples:
  // apiUrl: 'https://mybackend.onrender.com/api',
  // apiUrl: 'https://mybackend.fly.dev/api',
};
